<?xml version="1.0" encoding="utf-8"?>
<!-- Unused by the live layout (bar uses widget_search_bar_bg). Kept as a
     matching capsule so any leftover references stay a slim pill. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F21A1A22" />
    <corners android:radius="@dimen/widget_search_bar_radius" />
    <stroke
        android:width="1dp"
        android:color="#3D3D4A" />
</shape>
