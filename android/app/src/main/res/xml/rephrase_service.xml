<?xml version="1.0" encoding="utf-8"?>
<!-- canRetrieveWindowContent MUST be declared here (not in code) so the system
     warns the user at enable time. flagRetrieveInteractiveWindows is required to
     receive WINDOWS_CHANGED and to resolve the target field from getWindows(). -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewTextChanged|typeViewFocused|typeViewTextSelectionChanged|typeWindowStateChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="300"
    android:description="@string/bubble_service_desc" />
