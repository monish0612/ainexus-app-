<?xml version="1.0" encoding="utf-8"?>
<!-- Nexus AI web-search widget — a single-row 4x1 AMOLED pill. A gradient globe
     badge, an "ask the web" hint, and a gradient action button. Tapping anywhere
     on the bar launches the app straight into the AI web (online) search. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_search_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_search_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="10dp"
    android:paddingEnd="9dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <!-- Leading gradient globe badge -->
    <LinearLayout
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/widget_search_badge"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/widget_search_globe"
            android:layout_width="19dp"
            android:layout_height="19dp"
            android:contentDescription="@string/widget_search_label"
            android:src="@drawable/ic_widget_globe"
            android:tint="#FFFFFF" />
    </LinearLayout>

    <!-- Hint -->
    <TextView
        android:id="@+id/widget_search_hint"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="13dp"
        android:layout_marginEnd="10dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:text="@string/widget_search_hint_web"
        android:textColor="#8A93A8"
        android:textSize="15sp" />

    <!-- Trailing gradient action button -->
    <LinearLayout
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/widget_search_go_btn"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/widget_search_go"
            android:layout_width="17dp"
            android:layout_height="17dp"
            android:contentDescription="@string/shortcut_search"
            android:src="@drawable/ic_widget_search"
            android:tint="#FFFFFF" />
    </LinearLayout>

</LinearLayout>
