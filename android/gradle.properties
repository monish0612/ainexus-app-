org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.nonTransitiveRClass=true
# Off: Flutter plugin sources live in Pub cache on C: while this project is
# on D:. Kotlin incremental RelocatableFileToPathConverter then fails with
# "this and base files have different roots" when closing caches (pdfx, etc).
kotlin.incremental=false

